{
  "name": "vrisa-versions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "v1":  "cd v1 && npm run dev",
    "v2":  "cd v2 && npm run dev",
    "all": "concurrently \"cd v1 && npm run dev\" \"cd v2 && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
